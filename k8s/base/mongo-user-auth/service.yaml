apiVersion: v1
kind: Service
metadata:
  name: mongo-user-auth
  namespace: socialbeats
spec:
  selector:
    app: mongo-user-auth
  ports:
    - port: 27017
      targetPort: 27017
  clusterIP: None
