apiVersion: v1
kind: Service
metadata:
  name: analytics-and-dashboards
  namespace: socialbeats
spec:
  selector:
    app: analytics-and-dashboards
  type: ClusterIP
  ports:
    - port: 3003
      targetPort: 3003
